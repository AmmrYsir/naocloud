{
	"id": "system-health",
	"name": "System Health Monitor",
	"description": "Adds a dashboard widget showing disk, memory, and service health checks at a glance.",
	"version": "1.0.0",
	"author": "ServerPilot",
	"icon": "🩺",
	"contributes": {
		"navItems": [
			{
				"label": "Health",
				"href": "/",
				"icon": "<svg class=\"h-5 w-5\" fill=\"none\" viewBox=\"0 0 24 24\" stroke=\"currentColor\" stroke-width=\"2\"><path d=\"M22 12h-4l-3 9L9 3l-3 9H2\"/></svg>"
			}
		],
		"widgets": [
			{
				"key": "health-summary",
				"title": "System Health",
				"colSpan": 2,
				"placement": "dashboard"
			}
		],
		"apiRoutes": [
			{
				"method": "GET",
				"path": "/status",
				"auth": true
			}
		],
		"settings": {
			"title": "Health Monitor Settings",
			"fields": [
				{
					"key": "diskThreshold",
					"label": "Disk Usage Alert Threshold (%)",
					"type": "number",
					"default": 85,
					"description": "Warn when disk usage exceeds this percentage"
				},
				{
					"key": "memThreshold",
					"label": "Memory Usage Alert Threshold (%)",
					"type": "number",
					"default": 90,
					"description": "Warn when memory usage exceeds this percentage"
				},
				{
					"key": "checkServices",
					"label": "Monitor Services",
					"type": "text",
					"default": "docker,nginx,ssh",
					"description": "Comma-separated list of services to monitor"
				}
			]
		}
	}
}